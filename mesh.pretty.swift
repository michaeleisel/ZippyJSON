struct mesh.pretty {    let `batches`: [batches]
    let `colors`: [Int]
    let `indices`: [Int]
    let `influences`: [[Double]]
    let `morphTargets`: morphTargets
    let `normals`: [Double]
    let `positions`: [Double]
    let `tex0`: [Double]
}

struct morphTargets {}

struct batches {    let `indexRange`: [Int]
    let `usedBones`: [Int]
    let `vertexRange`: [Int]
}

